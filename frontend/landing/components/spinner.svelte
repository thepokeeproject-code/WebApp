<span
    class="spin size-8 border-3 border-solid rounded-[50%] inline-block dark:border-x-slate-400 border-x-slate-400 border-y-transparent"
></span>

<style>
    .spin {
        animation: rotation 800ms linear infinite;
    }

    @keyframes rotation {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }
</style>
